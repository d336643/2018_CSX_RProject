### practice_3_answer
### ptt_info_book

# ??????????????????
person.name <- c("Jiyuian", "Shawnroom", "Bigmoumou")
person.sex <- c("F", "M", "M")
person.id <- c("jiyuian520", "shawnn520", "moumou123")
person.days <- c(201, 37, 99)

# ??????data.frame()???????????????4???????????????person.df
person.df <- data.frame(person.name, person.sex, person.id,person.days)
  
# ??????str()??????person.df??????
str(person.df)

# ??????summary()??????person.df summary
summary(person.df)

# ??????person.df
person.df
# ??????person.df?????????
person.df[1, ]

# ??????person.df??????????????????
person.df[2, 3]

# ??????person.df???person.id??????(??????$)
person.df&person.id
#person.df[ ,person.id]

# ??????order()???person.df$person.days?????????, ??????days.position
days.postion <- order(person.df$days)
  
# ??????days.postion, ??????person.df
person.df[days.postion, ]
  
# ??????grepl()?????????person.df$person.id??????520?????????
spirit.520 <- grepl("520", person.df$person.id)
  
# ?????????520???????????????
person.df[spirit.520, ]